@model BOOKLOUD.Models.CourseDetailsModel
@{
    ViewData["Title"] = "Course Information";
    Layout = "_Layout";
}

<div class="container p-3">
    <h2>View Course</h2>
    <div>
        <h4>CourseDetailsModel</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CourseName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CourseName)
            </dd>
        </dl>
    </div>
    <div>
        <a asp-action="EditCourse" asp-route-id="@Model.Id">Edit</a> |
        <a asp-action="CourseManagement">Back to List</a>
    </div>
</div>